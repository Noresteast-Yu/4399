# 地铁跑酷换乘助手 SRS 完成度清单

更新时间：2026-06-10

## 已完善

| 编号 | 功能 | 当前实现 |
| --- | --- | --- |
| FR-001 | 首页展示 | Android 端首页包含快捷规划、地铁图、到站信息、常用入口和底部导航。 |
| FR-002 | 路线规划 | 后端支持上海地铁 1-18 号线网络换乘规划，可返回时间、分段、站数和换乘段。 |
| FR-003 | 地铁服务 | `/api/subway-service/lines` 返回 1-18 号线、站序、首末班、间隔、拥挤度演示字段；站点设施接口可用。 |
| FR-004 | 高铁引导 | 高铁车次接口和换乘引导接口提供车次、站点、车厢、站台和换乘建议演示数据。 |
| FR-005 | 换乘时间管理 | 支持开始换乘计时和进度更新接口，站内指引可给出赶车提示。 |
| FR-006 | 常用路线 | 支持默认用户常用路线查询、添加和删除。 |
| FR-007 | 出行提醒 | 支持出行提醒接口，提供晚点、管制、拥挤等演示提醒。 |
| FR-008 | 站点数据管理 | 数据库和内存网络均维护站点 ID、名称、城市、类型等信息。 |
| FR-009 | 线路数据管理 | 维护 1-18 号线名称、颜色、方向、站点数量和站序。 |
| FR-010 | 线路站序管理 | 后端内置 1-18 号线站序，数据库种子已补强 1 号线。 |
| FR-011 | 换乘规则管理 | 路线规划支持全网换乘；示例站点保留车厢建议和换乘提示。 |
| FR-012 | 平面换乘指引 | 同济大学站使用站内拓扑、文字步骤、站内真实照片和出口指引；其他站点使用通用演示指引。 |
| FR-013 | 数据校验 | `/api/data/validate` 校验数据库表、引用关系和 1-18 号线内存网络完整度。 |
| FR-014 | iOS 扩展预留 | Flutter 结构和后端 JSON 接口保持通用，可后续复用到 iOS。 |
| FR-015 | AI 语音辅助规划 | 首页新增 AI 语音助手弹窗和云朵悬浮窗，支持语音/手动输入终点、定位最近站、推荐进出站口并跳转路线规划。 |
| FR-016 | GPS 最近站推荐 | 后端新增 `/api/location/nearest-station`，根据经纬度返回最近地铁站、推荐入口、候选入口和距离。 |
| FR-017 | 语音目的地解析 | 后端新增 `/api/assistant/parse-destination`，将“我要去/导航到”等自然语言归一为站点名称和站点 ID。 |

## SRS 数据模块接口对照

| SRS 接口 | 当前接口 | 状态 |
| --- | --- | --- |
| getDefaultConfig() | `GET /api/data/default-config` | 已实现，返回默认城市、站点、线路、方向和 1-18 号线概要。 |
| listStations() | `GET /api/data/stations?keyword=` | 已实现，返回全网站点、城市、站点类型和可用线路。 |
| listLines() | `GET /api/data/lines` 或 `GET /api/subway-service/lines` | 已实现，返回 1-18 号线、方向、站序、首末班、间隔和拥挤度演示字段。 |
| listStationsByLine(lineId) | `GET /api/data/lines/{lineId}/stations` | 已实现，按 order 返回线路站点。 |
| searchTransferRules(keyword, originStationId, lineId) | `GET /api/data/transfer-rules` | 已实现，返回规则 ID、方向、站数、预计时间、车厢建议和可换乘线路。 |
| getRoutePlan(ruleId) | `GET /api/data/route-plan/{ruleId}` | 已实现，可将规则 ID 转成完整路线建议。 |
| validateData() | `GET /api/data/validate` | 已实现，校验数据库表和内存线路网完整性。 |
| getNearestStation(lat, lng) | `GET /api/location/nearest-station` | 已实现，供 AI 语音助手和定位推荐使用；数据库不可用时仍返回演示定位数据。 |
| parseDestination(text) | `POST /api/assistant/parse-destination` | 已实现，供 AI 语音助手解析用户口语目的地，前端保留本地兜底。 |

## 本次修复

- 修复内存地铁数据中的乱码字段，站点城市和类型统一返回“上海 / 地铁站”。
- 常用路线在数据库未连接时返回同济大学相关演示路线，首页不再空白。
- 静态资源在数据库未连接时返回图标、拓扑和站点示意图资源，满足 SRS 静态资源维护要求。
- 前端 API 层新增 SRS 数据模块接口调用方法。
- 前端网络异常统一使用简短中文提示，避免把 Dio 英文异常整段展示给用户。
- 服务配置页提供模拟器、本机和真机手填入口，并可直接测试后端连接、校验后端数据完整度。
- 个人页常用路线改为走统一 API 层，添加路线会带默认用户，演示路线会提示不可删除。
- 兼容旧站点 ID，如 `tongji_university` 可映射到全网线路数据中的同济大学站。
- 数据库连接但站点/设施数据不全时，会降级使用 1-18 号线内存网络和通用设施信息，避免服务页 404。
- `/api/data/validate` 在无数据库时也会校验内存线路网并返回 200，通过 `warnings` 标记当前为演示数据模式。
- 新增 `DELETE /api/users/{userId}` 和 `POST /api/users/{userId}/anonymize`，覆盖 NFR-005 中用户数据删除和匿名化预留要求。
- 新增 AI 语音助手首页入口，关闭后收起为左侧云朵悬浮窗；支持麦克风识别、终点站名解析、定位最近站和自动带入进出站口。
- 新增后端最近站推荐接口，前端定位后优先调用后端，失败时使用本地关键站点兜底。
- 新增 `station_geo_points` 数据表和 007 迁移，最近站推荐优先读取数据库坐标，未连接数据库时才使用内置关键站点兜底。
- 新增语音目的地解析接口，前端语音识别完成后优先调用后端解析，无法连接后端时使用本地站名匹配兜底。

## 当前仍属于演示数据的部分

- 首末班时间、发车间隔、拥挤度为课程演示字段，不代表官方实时运营数据。
- 高铁车次信息为本地演示数据，未接入铁路官方实时接口。
- 到站时间优先请求外部接口，失败时使用本地演示到站数据。
- 站内真实照片目前重点覆盖同济大学站；其他站点不再冒充真实照片，会使用后端生成的站点示意图，并标注为示意。
- 常用路线和用户设置使用默认用户或本地数据，没有正式登录系统。
- 最近站推荐当前覆盖同济大学、四平路、五角场、上海火车站、人民广场、南京东路、虹桥火车站、浦东国际机场等演示关键站；全量 GPS 需要继续给数据库所有站点补经纬度。

## 已验证

- 后端完整测试：`go test ./...`
- 覆盖测试包括：全网换乘规划、1 号线/5 号线换乘、线路列表、站点出口、站点/设施内存兜底、地铁到站降级、高铁演示数据、站内照片/示意图 URL、数据校验、SRS 数据模块接口、静态资源、常用路线、用户隐私接口和用户输入校验。
